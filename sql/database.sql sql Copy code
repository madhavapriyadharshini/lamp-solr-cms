CREATE DATABASE cms;

USE cms;

CREATE TABLE posts (
  id INT AUTO_INCREMENT PRIMARY KEY,
  title VARCHAR(255),
  body TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

INSERT INTO posts (title, body) VALUES
('Welcome to the CMS', 'This is a sample post.'),
('Another Article', 'CMS powered by Solr search!');
